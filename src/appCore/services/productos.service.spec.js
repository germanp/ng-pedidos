describe('productos', function() {

    beforeEach(module('ngPedidos'));

    it('should ...', inject(function(productosService) {

        //expect(productos.doSomething()).toEqual('something');

    }));

});
